# Project Structure

```text
can_video_demo_draft/
  config.json
  empirical_parameters.json
  run_demo.py
  demo_core/
    config.py
    diagnostics.py
    robot_control.py
    perception.py
    depth_vision.py
    fsm_types.py
    navigation.py
    tangentbug.py
    state_machine.py
    logging_utils.py
  assets/
    bin_apriltag_36h11_id_0.png
    models/detectnet_native_can/
    samples/can1.jpg
  tests/
    board_first_checks.py
    camera_network_diagnostics.ipynb
    full_demo_integration_test.ipynb
    validate_can_detectnet_image.py
    test_*.py
  tuning_tools/
    arm_sequence_tuning.ipynb
  logs/
  legacy_params/
    old_demo_params_reference.json
    logs/legacy_demo_success.log
    logs/legacy_demo_failure.log
```

## Ownership

- `run_demo.py` is the only complete demo entry point.
- `demo_core/state_machine.py` owns transitions, retries, pickup/release sequencing, and cleanup.
- `demo_core/fsm_types.py` defines mission states, events, target types, and shared mission context.
- `demo_core/navigation.py` implements the shared can/bin search, alignment, approach, and final verification rules.
- `demo_core/diagnostics.py` owns reusable camera and model lifecycle operations for notebooks.
- `demo_core/perception.py` exposes can, AprilTag, and depth observations.
- `demo_core/robot_control.py` owns base pulses and servo commands.
- `demo_core/depth_vision.py` owns the JetBot camera and depthNet lifecycle and depth observations.
- `demo_core/tangentbug.py` is an optional depth-profile avoidance planner.

The old generic module names are no longer part of the public structure: FSM definitions live in `fsm_types.py`, FSM execution lives in `state_machine.py`, robot commands live in `robot_control.py`, and camera/depth handling lives in `depth_vision.py`. `perception.py` retains its standard robotics name.

Mission progress is represented only by `MissionContext.completed_pickups`. `runtime.max_pickups` is the completion target; there is no separate score configuration.

`tests/` contains diagnostics and verification only. `tuning_tools/` contains deliberate parameter modification tools. Neither directory contains an alternative production FSM.

The can detector uses the standard jetson-inference `detectNet` API and exposes the same observation contract consumed by navigation. There is no alternative can inference backend in the active project structure.

## Generated Files

TensorRT `.engine` files generated by jetson-inference, notebook checkpoints, diagnostic images, and active logs are ignored.

The `legacy_params/` directory is read-only historical parameter context. Production code must not import from it.
